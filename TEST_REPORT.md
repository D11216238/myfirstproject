# 防呆計算機測試報告 (Safe Division Calculator Test Report)

## 專案說明

這是一個防呆計算機專案，實作了 `safe_division(a, b)` 函式，能夠安全地處理除法運算，防止「除以零」錯誤。

## 功能特色

- ✅ 安全的除法運算
- ✅ 防止除以零錯誤
- ✅ 完整的單元測試覆蓋
- ✅ 支援正數、負數、浮點數運算

## 檔案結構

```
.
├── safe_division.py        # 安全除法函式實作
├── test_safe_division.py   # 單元測試
└── TEST_REPORT.md          # 測試報告（本檔案）
```

## 如何執行

### 執行測試
```bash
python3 -m unittest test_safe_division.py -v
```

### 使用函式
```python
from safe_division import safe_division

# 正常相除
result = safe_division(10, 2)  # 返回 5.0

# 除以零（防呆機制）
result = safe_division(10, 0)  # 返回 None，不會出錯
```

## 測試結果

### 🟢 綠燈情境（所有測試通過）

當 `safe_division` 函式包含完整的防呆機制時，執行單元測試的結果：

```
test_boundary_values (test_safe_division.TestSafeDivision.test_boundary_values)
測試邊界值 ... ok
test_division_by_zero (test_safe_division.TestSafeDivision.test_division_by_zero)
測試除以零的情況（防呆機制） ... ok
test_float_division (test_safe_division.TestSafeDivision.test_float_division)
測試浮點數相除 ... ok
test_negative_numbers (test_safe_division.TestSafeDivision.test_negative_numbers)
測試負數相除 ... ok
test_normal_division (test_safe_division.TestSafeDivision.test_normal_division)
測試正常的數值相除 ... ok
test_very_small_numbers (test_safe_division.TestSafeDivision.test_very_small_numbers)
測試非常小的數值 ... ok
test_zero_dividend (test_safe_division.TestSafeDivision.test_zero_dividend)
測試被除數為零的情況 ... ok

----------------------------------------------------------------------
Ran 7 tests in 0.000s

OK
```

**結論**：所有 7 個測試案例都通過 ✅
- 正常數值相除 ✅
- 負數相除 ✅
- 邊界值相除 ✅
- 浮點數相除 ✅
- 零當被除數 ✅
- 除以零的防呆處理 ✅

### 🔴 紅燈情境（測試失敗）

當我們移除或註解掉 `safe_division` 函式中處理除以零的程式碼時：

**修改前（有防呆機制）：**
```python
def safe_division(a, b):
    if b == 0:  # 防呆機制
        return None
    return a / b
```

**修改後（移除防呆機制）：**
```python
def safe_division(a, b):
    # if b == 0:  # 防呆機制被註解
    #     return None
    return a / b  # 直接執行除法，會出錯！
```

**測試結果：**
```
test_division_by_zero (test_safe_division.TestSafeDivision.test_division_by_zero)
測試除以零的情況（防呆機制） ... ERROR

======================================================================
ERROR: test_division_by_zero (test_safe_division.TestSafeDivision.test_division_by_zero)
測試除以零的情況（防呆機制）
----------------------------------------------------------------------
Traceback (most recent call last):
  File "test_safe_division.py", line 29, in test_division_by_zero
    self.assertIsNone(safe_division(10, 0))
  File "safe_division.py", line 26, in safe_division
    return a / b
           ~~^~~
ZeroDivisionError: division by zero

----------------------------------------------------------------------
Ran 7 tests in 0.001s

FAILED (errors=3)
```

**結論**：測試失敗 ❌
- 程式拋出 `ZeroDivisionError` 異常
- 針對除以零的測試案例全部失敗
- 程式未能妥善處理除以零的情況

## 測試案例說明

### 1. test_normal_division
測試正常的數值相除，確保基本除法功能正確。

### 2. test_negative_numbers
測試負數相除的各種情況，包括負負得正。

### 3. test_division_by_zero ⭐ 關鍵測試
**這是最重要的測試**，驗證當除數為零時，函式能返回 `None` 而不是拋出異常。

### 4. test_zero_dividend
測試當被除數為零時的正常行為（結果應為 0）。

### 5. test_boundary_values
測試邊界值，如 1/1、大數值等。

### 6. test_float_division
測試浮點數的精確度和正確性。

### 7. test_very_small_numbers
測試極小數值的處理能力。

## 安全性分析

### 防呆機制的重要性

1. **預防程式崩潰**：沒有防呆機制時，除以零會導致 `ZeroDivisionError`，使程式終止。
2. **提升使用者體驗**：返回 `None` 讓調用方可以優雅地處理錯誤。
3. **符合防禦性程式設計**：預先檢查潛在錯誤，而不是等待錯誤發生。

### 程式安全官的職責達成 ✅

✅ 實作了 `safe_division(a, b)` 函式  
✅ 加入除以零的防呆機制  
✅ 使用工具生成完整的單元測試  
✅ 執行測試並記錄結果  
✅ 驗證綠燈情境（所有測試通過）  
✅ 驗證紅燈情境（移除防呆後測試失敗）  

## 結論

本專案成功實作了防呆計算機，通過完整的單元測試驗證了防呆機制的有效性。測試結果清楚展示：
- 有防呆機制時：所有測試通過（綠燈 🟢）
- 無防呆機制時：除以零測試失敗（紅燈 🔴）

這證明了防呆機制對於確保程式安全運行的重要性。
